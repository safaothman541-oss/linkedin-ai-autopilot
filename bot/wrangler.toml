name = "ai-bot"
main = "worker.js"
compatibility_date = "2025-01-01"
workers_dev = true

# Public channel/group where published LinkedIn posts are mirrored.
[vars]
TELEGRAM_GROUP_ID = "@vertex1_tech"

# OPTIONAL daily auto-drafts: uncomment to have the bot send the 3 posts every day
# at 08:00 UTC (≈ 11:00 Iraq). It only SENDS drafts — publishing still needs you to
# reply to a draft with your image. Leave commented to keep the bot fully manual.
# [triggers]
# crons = ["0 8 * * *"]
